# Patterns to suppress from `mix dialyzer`. Each entry is a string
# (substring match against the warning) or a {file, type, line} tuple.
# Keep this list small and explain each entry — silencing without a
# reason just hides bugs.
[]
